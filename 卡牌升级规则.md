# 卡牌升级、装备、计策与出售交互规则

本文档记录网页演示版的武将卡叠加、装备穿戴、装备交换、计策拖拽目标、拖拽出售规则和代码修改入口。后续调整升级数值、等级上限、装备槽、计策目标、出售收益、表现或触发效果时，以本文档为基础同步修改。

具体武将基础攻击、生命和技能数据以 `游戏数据速查.md` 为唯一数据源；不得从旧设计文档或网页现有数值反推正式数据。完整属性与合并语义参考 `D:\三国自走棋\design\武将.md`，本文件将其中网页已实现部分写成可直接执行的规则。

## 1. 当前实现范围

- 仅武将卡可以叠加；计策卡不参与升级。
- 只有名称完全相同的武将卡可以叠加。
- 新购买并进入阵容的武将默认为 `1级0经验`，占用 1 个阵容槽。
- 商店中的同名武将可以拖到阵容中已有的同名武将上，购买后直接叠加，不额外占用阵容槽。
- “购买”指商店阶段玩家取得任意商店卡；“招募”专指购买武将卡，两者描述的是同一个购买事实。
- 购买武将进入空阵容位会同时产生召唤；购买同名武将并直接叠加时属于招募，但不属于召唤。
- 不同名武将不能叠加；拖到不同名武将上时显示红色阻塞提示。
- 3级为当前版本最高等级。3级武将不能继续接收同名卡；同名卡仍可拖到空阵容槽，作为新的 `1级0经验` 武将购买。
- 合并不会自动发生。玩家必须把商店卡或阵容中的武将主动拖到目标同名武将上。
- 每名阵容武将当前有 1 个装备槽；装备槽只保存 1 件装备。
- 装备可以购买并穿戴，也可以在阵容武将之间移动或交换；7 件普通商店装备和袁术生成的特殊装备“传国玉玺”均已接入结构化战斗效果。
- 计策不使用购买按钮，统一拖到阵容区域使用；10 张计策均已接入结构化真实效果。
- 阵容卡牌不显示常驻出售按钮。只有拖拽已拥有的武将或装备时，商店面板才显示出售区域。
- 商店卡牌可以通过鼠标右键切换锁定状态；锁定卡在刷新时保留，并按卡牌类型重新靠边排列。
- 商店阶段如果阵容中存在名称完全相同的武将，商店里的全部对应同名武将卡显示低强度绿色柔光提示；提示只做缓慢、轻微的呼吸变化，不显示额外文字，不改变购买或叠加规则。

## 2. 张数、等级与经验

`累计张数`包含最初进入阵容的第 1 张卡。

| 累计同名卡 | 等级 | 当前经验 | 距离下一级 |
|---:|---:|---:|---:|
| 1 | 1 | 0 | 还需 2 张 |
| 2 | 1 | 1 | 还需 1 张 |
| 3 | 2 | 0 | 还需 3 张 |
| 4 | 2 | 1 | 还需 2 张 |
| 5 | 2 | 2 | 还需 1 张 |
| 6 | 3 | 0 | 已满级 |

换算关系：

- 1级起点为累计 1 张，升到 2级需要累计 3 张。
- 2级起点为累计 3 张，升到 3级需要累计 6 张。
- 商店中的单张同名卡叠加时，累计张数增加 1。
- 两个阵容武将合并时，累计张数直接取双方原有累计张数之和，再根据上表重新计算等级和经验。
- 升级后经验归零；例如累计张数从 2 变为 3 时，状态从 `1级1经验` 变为 `2级0经验`。

计策或技能直接给予的经验与物理合入张数分开保存，但属性成长使用同一份总进度：

- `copies` 只记录累计合入的同名卡张数；
- `bonusExperience` 记录计策或技能直接给予的额外经验；
- 等级进度使用 `copies + bonusExperience` 计算，仍按累计进度 1/3/6 对应 1/2/3 级；
- 每 1 点直接经验在属性上等价于合入 1 张同名卡，因此也产生 `+1/+1` 堆叠成长；
- 一次获得多点直接经验时逐点结算，每实际获得 1 点分别触发一次经验事件；达到 3 级后，后续每 1 点溢出经验转化为 `+1/+1`，转化点不增加经验也不触发经验事件，但给予经验的能力仍视为生效；
- 直接经验不会伪造 `copies`，但堆叠成长统一按 `copies + bonusExperience - 1` 计算；
- 两个阵容武将合并时，双方 `copies` 和 `bonusExperience` 分别相加，再重新计算等级与当前经验；
- 达到 3 级后不能继续积累经验，也不能继续合入同名卡；计策或技能仍可给予经验，其溢出部分按每点 `+1/+1` 结算。
- 经验总进度最低为 1，不允许通过减经验低于“累计 1 张、1 级 0 经验”。刘备等减经验效果在无法继续减少时仍结算技能中的属性加成。

## 3. 购买与拖拽流程

### 3.0 商店卡牌锁定与刷新

1. 仅商店阶段允许右键切换卡牌锁定状态；右键操作不得启动卡牌拖拽。
2. 锁定状态保存在当前商店卡牌实例上。手动刷新和下一回合免费刷新都保留锁定卡，只重新生成各类型剩余的普通名额。
3. 刷新后，锁定武将保持彼此的相对顺序并移动到商店最左侧；锁定装备和计策保持彼此的相对顺序并移动到商店最右侧。
4. 锁定卡数量超过当前回合对应类型的普通槽位数量时，所有锁定卡仍须保留，超出部分使用商店中间溢出位。
5. 未锁定的升级奖励卡与其他未锁定卡一样会被刷新替换；锁定后的升级奖励武将按锁定武将处理。
6. 卡牌购买、合并购买、装备穿戴或计策成功使用后从商店移除，锁定状态不传入阵容武将或装备。
7. 锁定卡覆盖银色半透明遮罩并显示“锁定”标记；再次右键解除后立即移除遮罩。

### 3.1 拖到空阵容槽

1. 检查当前是否为商店阶段。
2. 检查金币是否足够。
3. 扣除卡牌费用并移除商店卡。
4. 创建 `1级0经验、累计1张` 的阵容武将。
5. 依次产生购买、招募和召唤事件，结算该武将的对应效果。

### 3.2 拖到已有同名武将

1. 检查名称是否完全相同。
2. 检查目标武将是否低于 3级。
3. 扣除卡牌费用并移除商店卡。
4. 目标武将累计张数加 1。
5. 重新计算等级和经验，并在达到累计 3 张或 6 张时升级。
6. 产生购买和招募事件，结算该武将的对应效果；本次不产生召唤事件。

### 3.3 阵容内移动、挤位、交换与同名合并

阵容武将拖到另一个阵容槽时：

1. 目标为空位：移动到空位。
2. 目标已有武将时，最近一次明确的水平拖动方向优先决定挤位方向：
   - 从右向左拖入目标槽时立即尝试向左挤位，从左向右拖入时立即尝试向右挤位，不要求指针越过目标槽中心线；
   - 水平位移累计达到约 `8px` 才确认方向；方向确认后持续生效，直到出现足够明确的反向拖动，避免手部小幅抖动造成方向反复切换；
   - 拖拽来源槽在本次判断中视为空位；
   - 从目标槽向指定方向寻找最近空位；
   - 找到空位后，目标与空位之间的武将依次向该空位移动，来源武将插入目标槽；
   - 指定方向没有空位时，不执行挤位，继续按下述交换或合并规则处理。
3. 尚未形成明确水平拖动方向时，以目标槽外侧各约 `32%` 的左右挤位区作为兜底；指针处于中部约 `36%`，或对应方向无法挤位时：
   - 目标为异名武将：双方交换位置；
   - 目标为同名武将：不交换位置，来源武将合并到目标武将。
4. 同名武将也可以通过明确的水平拖动方向或左右挤位区调整顺序；只有拖动方向不明确且落在目标槽中部时才进入合并判断。
5. 合并以目标武将为主体，来源武将从原阵容槽消失。
6. 合并后的累计张数等于双方原有累计张数之和，不额外增加卡牌数或经验。
7. 合并后“累计张数 + 直接经验”超过 6 时，本次合并失败，双方位置和状态都不变化。

### 3.4 属性合并顺序

1. 分别读取目标武将和来源武将的本体攻击、生命；当前堆叠成长和直接属性不能参加本体比较。
2. 新本体攻击为双方本体攻击的最大值，合并本身不额外增加本体攻击。
3. 新本体生命为双方本体生命的最大值，合并本身不额外增加本体生命。
4. 累计张数取双方之和，不额外加 1。
5. 双方直接经验相加，使用“合并后累计张数 + 合并后直接经验”重新计算等级与当前经验。
6. 装备槽和状态槽分别执行目标优先：目标对应槽为空时，才继承来源槽；未被继承的来源随来源武将销毁。
7. 额外羁绊不作为单张武将实例的合并槽处理，而是读取该武将本局的同名卡牌定义。
8. 根据“新累计张数 + 新直接经验”重新计算堆叠成长。
9. 汇总合并后仍然保留的装备、状态等直接属性。
10. 计算最终面板并执行攻击、生命上限校验。

## 4. 属性模型

### 4.1 三层属性

```text
最终攻击 = 本体攻击 + 堆叠成长攻击 + 直接攻击
最终生命 = 本体生命 + 堆叠成长生命 + 直接生命
```

- 基础属性：来自“武将”工作表，是本体属性的初始值。
- 本体属性：基础属性以及技能、计策等间接永久成长；合并时取双方对应本体属性的最大值，合并本身不产生额外本体成长。
- 堆叠成长：`累计张数 + 直接经验 - 1`，攻击和生命分别增加相同数值；每 1 点直接经验等价于 1 张同名卡的属性贡献。
- 直接属性：装备、状态等可以随来源移除而直接扣除的属性，不计入本体比较。
- 页面显示的攻击、生命只是计算结果，任何效果不得直接把面板值当作本体值保存。

### 4.2 网页字段

| 字段 | 含义 |
|---|---|
| `baseAttack` / `baseHealth` | 正式数据源中的初始属性 |
| `bodyAttack` / `bodyHealth` | 当前本体属性，不含堆叠和直接属性 |
| `directModifiers` | 按装备、状态等来源保存的直接属性 |
| `copies` | 当前累计张数 |
| `attack` / `health` | 重算后的最终面板属性 |

### 4.3 当前网页实例

| 情形 | 本体合并 | 堆叠成长 | 最终属性 |
|---|---:|---:|---:|
| 两张全新鲁肃 `3/3` | `3/3` | `+1/+1` | `4/4` |
| 两张全新太史慈 `5/3` | `5/3` | `+1/+1` | `6/4` |
| 两张全新关羽 `10/8` | `10/8` | `+1/+1` | `11/9` |

多个已经叠加过的阵容武将合并时，先从各自面板中排除堆叠成长和直接属性，再比较保存的本体属性。不得把旧堆叠成长重复算入新本体。

阵容内合并的完整属性公式为：

```text
新本体攻击 = max(目标本体攻击, 来源本体攻击)
新本体生命 = max(目标本体生命, 来源本体生命)
新累计张数 = 目标累计张数 + 来源累计张数
新直接经验 = 目标直接经验 + 来源直接经验
新堆叠成长 = 新累计张数 + 新直接经验 - 1
最终攻击 = 新本体攻击 + 新堆叠成长 + 合并后保留的直接攻击
最终生命 = 新本体生命 + 新堆叠成长 + 合并后保留的直接生命
```

最终攻击力最低为 1、最高为 50；最终生命上限为 50。张飞等减攻效果不能使攻击力低于 1。

## 5. 拖拽视觉规则

- 页面和卡牌图片关闭浏览器原生文字/图片选择，拖拽时不得出现蓝色选中色块或原生图片拖拽残影。
- 开始拖拽武将卡、装备卡、计策卡、阵容武将或已穿戴装备时，直接将原卡牌/装备节点从原槽临时提到页面拖拽层，原槽不再保留第二张可见副本。
- 指针开始移动后，原节点跟随指针移动，并保持拖拽前的可见比例；不得额外克隆跟随预览。
- 跟随指针的原节点不参与命中检测，也不得提前改变卡牌业务状态或落点判断。
- 松手完成、松手取消或收到 `pointercancel` 时，先把原节点恢复到原槽；只有结算成功后，业务逻辑才可以通过重新渲染移除或移动该卡牌。
- 可放入的空槽使用金色描边。
- 可叠加的同名武将使用绿色描边。
- 未拖拽时，商店中与当前阵容武将名称完全相同的武将卡使用低强度绿色柔光和缓慢呼吸提示；离开商店阶段或阵容不再存在该名称时立即移除。
- 商店武将拖到不同名武将或已满级同名武将时使用红色描边。
- 阵容武将没有明确水平拖动方向并停在目标槽中部时，异名交换使用金色描边，同名合并使用绿色描边。
- 阵容武将以明确的水平方向拖入占用槽且对应方向存在空位时，无需越过目标中心线就显示金色方向箭头；受影响卡牌向该方向轻微偏移，最终承接挤位的空槽显示金色虚线。尚未形成明确方向时，目标槽左右挤位区继续提供相同预览。
- 单体计策拖到有武将的阵容槽使用金色描边，拖到空阵容槽使用红色描边。
- 非单体计策拖到任意阵容槽都使用金色描边。
- 指针被系统取消时，必须清空当前拖拽状态和所有落点提示。

### 5.1 卡牌顶部等级与经验条

- 外形与位置参考 Unity Prefab：`Assets/Prefab/Draft/HeroCard2.prefab`。
- Prefab 基准尺寸为 `228 × 296`，与网页武将卡尺寸一致。
- 左侧等级文字参考 `Level_Text_Anchor`，显示为 `LV1`、`LV2`、`LV3`。
- 经验条底图使用 `res/ShopPanel/exp-bar-base.png`。
- 经验条填充图使用 `res/ShopPanel/exp-bar-fill.png`。
- 填充图不允许横向压缩变形，使用从右向左裁切的方式表示进度。
- 1级经验进度为 `0/2`、`1/2`；2级经验进度为 `0/3`、`1/3`、`2/3`；3级显示满条。
- 商店中尚未购买的卡牌不显示等级和经验条，只有阵容武将显示。

### 5.2 右上角装备槽

- 装备槽外形与位置参考 `D:\三国自走棋\empire_gambit_client\Assets\Prefab\Draft\HeroCard2.prefab`。
- Prefab 中 `Equipment_Slot` 相对卡牌中心的锚点为 `(94.3, 127.6)`，尺寸为 `62.7 × 62.7`；网页按 `63 × 63` 呈现在卡牌右上角。
- Prefab 中装备图标尺寸为 `36.5 × 36.5`；网页按 `37 × 37` 显示。
- 只有阵容武将显示装备槽；商店武将卡不显示装备槽。
- 空槽显示为空装备位；已穿戴时显示装备图标，悬停可查看名称和描述。
- 阵容卡牌按从左到右递减的层级排列，避免右侧卡牌遮住左侧卡牌向外延伸的装备槽。
- 从商店拖入装备和拖拽已穿戴装备时，合法目标使用金色描边，非法目标使用红色描边。

### 5.3 攻击、生命与金币区域

- 武将卡整体继续使用 `228 × 296` 基准尺寸，不调整名称、头像、羁绊、技能提示和装备槽等其他区域。
- 攻击与生命区域同时参考 `HeroCard0.prefab` 和 `HeroCard2.prefab`；两个 Prefab 中对应节点的位置和尺寸一致。
- 攻击底图 `Attack_Icon` 的中心锚点为 `(-67, -122.3)`，尺寸为 `94 × 47`；攻击数值 `Attack_Value_Anchor` 的中心锚点为 `(-46, -127.3)`，尺寸为 `48 × 28`。
- 生命底图 `Health_Icon` 的中心锚点为 `(67.3, -121.5)`，尺寸为 `87 × 45`；生命数值 `Health_Value_Anchor` 的中心锚点为 `(85, -127.3)`，尺寸为 `48 × 28`。
- 商店金币区域参考 `HeroCard0.prefab`：`Gold_Icon` 的中心锚点为 `(-85.567, 124.5)`，尺寸约为 `56.9 × 56`；`Gold_Text` 的中心锚点为 `(-87.4, 121.2)`，尺寸约为 `35.4 × 19.9`。
- 网页将 Unity 中以卡牌中心为原点的坐标换算为左上角定位；攻血图标保持原始宽高比，数值文字单独按各自锚点定位，不再和底图共用中心点。
- Unity 文字锚点包含其字体基线补偿，不能直接作为网页字体的视觉中心。网页金币数字改为在完整金币图标内居中；攻击数字在攻击底图右侧数值框 `left: 40px; top: 7px; width: 54px; height: 34px` 内居中；生命数字在生命底图右侧数值框 `left: 36px; top: 7px; width: 51px; height: 32px` 内居中。

### 5.4 阵容卡牌间距与缩放

- 阵容面板固定显示 5 个卡位，相邻卡位中心间距为 `210px`。
- 武将卡基准尺寸仍为 `228 × 296`；只在阵容区域将整张卡以 `78%` 比例显示，最终可见卡体约为 `177.8 × 230.9`。
- 缩放作用于完整武将卡节点，因此名称、头像、羁绊、星级、等级经验、攻血、技能提示和装备槽全部保持原有相对比例；不得只缩字体。
- 阵容卡体之间约保留 `32px`，上一张卡向右延伸的装备槽与下一张卡体之间约保留 `13px`，五个装备槽不得互相叠压。
- 商店武将卡、装备/计策卡和拖拽命中槽位尺寸不随阵容卡缩放；阵容拖拽仍以原来的 5 个卡位区域判断落点。

### 5.5 阵容状态标签

- 商店阶段的阵容武将卡必须显示当前状态标签，并与战斗快照卡使用同一套状态名称和标签样式。
- 状态标签只读取该卡当前实际持有的状态；商店阵容不显示仅存在于战斗副本中的临时状态。
- 技能禁用等可展示状态也使用同一标签区域，卡牌无状态时不占用标签空间。

## 6. 装备、计策与出售

### 6.1 从商店购买装备

1. 装备卡不使用“购买”按钮，玩家必须把装备拖到阵容中的武将。
2. 检查当前是否为商店阶段、金币是否足够。
3. 目标必须已有武将，并且该武将的装备槽必须为空。
4. 检查通过后扣除装备费用，从商店移除装备，并把装备写入目标武将的装备槽。
5. 如果目标没有武将，或已有装备，本次购买失败且不扣金币、不移除商店卡。
6. 为避免静默销毁旧装备，商店装备不能直接覆盖已穿戴装备；玩家应先移动、交换或出售原装备。
7. 装备统一保存名称、图片、阶级、描述和结构化效果 ID；7 件普通商店装备及传国玉玺的战斗状态均保存在本场战斗副本。只有传国玉玺的商店生成加成为明确允许的跨阶段写回。

当前装备效果：

| 装备 | 结构化效果 |
|---|---|
| 令箭 | 持有者的武将技能与其他武将技能处于相同触发条件时，额外获得最高优先级；同优先级内仍按当前攻击力动态排序 |
| 兵符 | 持有者阵亡事件结算时，在己方最前方召唤 1 名 `2/1` 骑兵 |
| 绝影 | 持有者受到 `attack` 或 `skill` 普通伤害时 `-2`，最低为 0 |
| 百厄角 | 持有者完成本次同时攻击后，重新读取敌方最前方的存活单位并施加 1 种随机负面状态；持有者在本次交锋中阵亡不取消已取得的攻击后效果 |
| 神农秘典 | 持有者每次获得负面状态，或每次实际受到大于 0 的伤害后，自身 `+2/+2`；两个事件连续发生时可分别触发 |
| 传国玉玺 | 特殊生成装备；佩戴者阵亡时，使商店中佩戴者羁绊武将永久 `+1/+1`。袁术自身佩戴时，1/2/3 级分别总共结算 2/3/4 次 |
| 蚩尤古盾 | 每场战斗前 2 次受到普通伤害时各抵挡最多 10 点；伤害不足 10 点时仍消耗 1 次；不抵挡灼烧、引燃等真实伤害 |
| 修罗刀 | 每场战斗第 1 次普通攻击伤害 `+20`，之后失效 |

普通骑兵统一定义为 `2/1`、无技能，基础阵营继承召唤者；额外羁绊和临时羁绊不继承。百厄角在攻击后效果实际结算时重新读取敌方最前方的存活单位，不锁定本次普通攻击原目标；原目标在同时攻击中阵亡时，百厄角会改为作用于此时成为最前方的下一名敌军。其随机负面状态池包含当前通过统一负面状态接口实现的全部状态：灼烧、震慑、反间。无双和休整是正面状态，吕蒙的本轮技能禁用是独立交锋控制，均不进入该池。每名武将只有 1 个状态槽，新状态覆盖旧状态，同名状态不叠层。百厄角施加灼烧时，即使没有东吴业火，也会在每次交锋后造成 1 点真实伤害，并可因再次获得任意负面状态触发引燃；施加震慑时使目标造成的非真实伤害减半；施加反间后，目标阵亡时会在百厄角持有者一方队首以 `LV1 3/3` 加入，基础阵营继承持有者。

传国玉玺不进入普通商店随机装备池。招募袁术时，若袁术装备槽为空，则生成并直接佩戴 1 件传国玉玺；若装备槽已有其他装备，则保留原装备且不生成玉玺，避免静默覆盖或销毁。玉玺生成后使用与普通装备完全相同的移动、交换和出售交互，因此可以交给周瑜等其他武将佩戴。

### 6.2 阵容内移动与交换装备

1. 玩家从武将卡右上角的已穿戴装备开始拖拽。
2. 拖到另一名有武将的阵容槽：
   - 目标装备槽为空：装备从来源武将移动到目标武将；
   - 目标装备槽已有装备：来源与目标的两件装备互换。
3. 拖到空阵容槽或阵容外普通区域：不发生变化。
4. 拖回原武将：不发生变化。
5. 移动与交换装备不消耗金币。

### 6.3 拖拽出售

1. 阵容卡牌不显示出售按钮。
2. 玩家开始拖拽阵容武将或已穿戴装备时，商店面板区域显示半透明出售覆盖层，并覆盖整个商店卡牌面板；底层商店内容仍可隐约辨认。
3. 把已拥有的武将或装备拖入出售区域并松手，才执行出售；拖到其他区域则不出售。
4. 商店中的未购买卡牌不属于玩家，拖拽时不显示出售区域，也不能出售。
5. 出售武将获得等同于当前等级的金币：1级获得 1 金币、2级获得 2 金币、3级获得 3 金币。
6. 出售武将会同时移除该武将及其已穿戴装备；被一并移除的装备不额外获得金币。
7. 出售单件装备不获得金币，武将保留并清空装备槽。
8. 出售只允许在商店阶段进行；金币仍受当前金币上限约束。

每次购买司马徽时，必须为 1 名友军完成一次不可取消的永久额外羁绊选择，并使该目标获得 `+（1/1）` 本体成长。直接叠加购买时，先完成同名合并与升级，再按购买完成后的司马徽等级结算目标属性成长。

### 6.4 与武将合并的关系

- 同名武将合并仍以落点武将为目标主体。
- 目标武将装备槽为空、来源武将有装备时，来源装备继承到目标。
- 目标武将已有装备时，保留目标装备；来源装备随来源武将销毁。
- 这一合并继承规则与“玩家主动拖拽装备进行交换”是两套独立操作，合并不会自动交换装备。

### 6.5 计策拖拽使用

1. 所有计策卡均取消“购买”按钮，玩家必须从商店拖到阵容区域使用。
2. 计策按第一工作表中的描述分为两类：
   - `unit`：描述明确要求“选择1个武将”，属于单体目标计策；
   - `lineup`：随机武将、多个武将、全体武将或选择羁绊等不由落点指定单个武将的效果，属于非单体计策。
3. 单体目标计策必须拖到已有武将的阵容卡上；拖到空阵容位、阵容外或商店出售区域都不结算。
4. 非单体计策拖到任意阵容位松手即可触发；目标阵容位可以有武将，也可以为空。
5. 合法落点后先检查计策是否存在有效目标；需要选择羁绊的计策打开羁绊选择弹窗，玩家可以取消。
6. 所有计策价格统一为 3 金币；只有选择完成且效果能够实际结算时，才扣除 3 金币、移除商店计策并记录使用日志。
7. 非法落点、金币不足、没有有效目标或取消羁绊选择时，不扣金币、不移除计策。

| 目标类型 | 当前计策 |
|---|---|
| 单体目标 `unit` | 演练、口才、歃血盟书、开悟、聚势强军 |
| 非单体 `lineup` | 举贤、同袍共进、潜龙蓄势、整军经武、同心戮力 |

计策目标分类同时保存在 `游戏数据速查.md` 和 `game-data.js` 的 `targetMode` 字段；前者是权威数据，后者是网页运行副本，两者必须保持一致。

全部计策按以下口径结算：

- 演练：所选武将本体永久 `+1/+1`。
- 口才：为所选武将选择一个当前未拥有的额外羁绊，并写入该武将本局的整个同名卡牌定义；当前阵容、当前商店以及本回合后续刷新或奖励出现的所有同名武将同步获得该临时羁绊，进入下一回合商店时统一清除。
- 举贤、同袍共进：先找出当前等级最高且已激活的羁绊；多个羁绊并列时随机选择其中一个，再在该羁绊武将中不放回随机 1/2 名目标。
- 潜龙蓄势：羁绊等级为 0 视为未激活；拥有任一未激活羁绊的阵容武将获得一次 `+1/+1`，双羁绊武将不会重复获得。
- 整军经武：玩家可从四个羁绊中选择一个，使本局商店的该羁绊武将获得累计 `+2/+2` 生成加成；当前商店卡立即重算，后续普通刷新和升级奖励生成的对应羁绊武将也读取累计值。对同一羁绊使用 2 次后，该羁绊的商店武将每次生成均为累计 `+4/+4`；购买后该次生成属性随卡进入阵容。
- 歃血盟书：本局第一次成功使用时，为所选武将选择一个永久额外羁绊，并同步修改本局全部同名卡牌定义；第二次及以后每次使所选武将本体永久 `+3/+3`。使用次数按本局全局计数，不按目标分别计数。
- 同心戮力：从当前等级最高且已激活的羁绊中选择一个；并列时随机，使该羁绊下当前全体阵容武将永久 `+1/+1`。
- 开悟：所选武将获得 1 点直接经验；不增加物理合入张数，但属性上等价于合入 1 张同名卡，立即增加 `+1/+1` 堆叠成长，并可能触发升级、蜀羁绊和升级奖励。目标经验已满时仍可使用，该点溢出经验直接转化为 `+1/+1`。
- 聚势强军：本局第一次成功使用为 `+3/+3`，第二次为 `+4/+4`，之后每次继续各增加 1；使用次数按本局全局计数。

### 6.6 结构化效果

- 10 张计策在 `game-data.js` 中均使用稳定效果 ID，通过 `stratagem:use` 商店事件结算。
- 效果 ID 依次为：`stratagem.encourage`、`stratagem.temporary-bond`、`stratagem.recommend-talent`、`stratagem.advance-together`、`stratagem.hidden-potential`、`stratagem.train-army`、`stratagem.blood-oath`、`stratagem.united-force`、`stratagem.master-guidance`、`stratagem.gather-strength`。
- 7 件普通商店装备及 1 件特殊装备依次使用稳定效果 ID：`equipment.initiative-flag`、`equipment.cavalry-talisman`、`equipment.iron-armor`、`equipment.calamity-blade`、`equipment.blood-armor`、`equipment.imperial-jade-seal`、`equipment.black-tortoise-shield`、`equipment.siege-crossbow`。
- 装备效果分别复用 `battle:start`、`attack:after`、`damage:before`、`damage:after`、`status:apply` 和 `unit:death` 事件。
- 已接入效果不得通过匹配或解析中文描述执行。
- 通用事件、同时攻击、动态技能排序、伤害管线和连锁触发规则以《技能结算规则.md》为准。

## 7. 升级字段与修改入口

阵容武将使用以下升级字段：

| 字段 | 含义 |
|---|---|
| `copies` | 当前武将累计合入的同名卡张数 |
| `bonusExperience` | 计策或技能直接给予的额外经验；每点同时产生一张同名卡等价的 `+1/+1` 堆叠成长 |
| `level` | 当前等级，现为 1～3 |
| `experience` | 当前等级内已经获得的经验 |

本局级计策状态另外保存在：

| 字段 | 含义 |
|---|---|
| `heroBondDefinitions` | 按武将名称保存本局永久/临时额外羁绊，供当前阵容、当前商店和未来生成卡共同读取 |
| `shopBondBonuses` | 按羁绊保存整军经武累计的商店生成攻击/生命加成 |

当前升级阈值集中定义在 `app.js` 的 `UNIT_LEVEL_COPY_THRESHOLDS`：

```js
{
  1: 1,
  2: 3,
  3: 6
}
```

后续如果修改等级上限或累计张数，必须同时检查：

- `MAX_UNIT_LEVEL`
- `UNIT_LEVEL_COPY_THRESHOLDS`
- `getUnitProgress`
- `getUnitProgressValue`
- `getUnitStackGrowth`
- `getHeroBondDefinition`
- `syncHeroBondDefinitionInstances`
- `getShopBondBonus`
- `syncShopHeroCardBonuses`
- `getExperienceNeeded`
- `canMergeCardIntoUnit`
- `canMergeUnits`
- `mergeUnitIntoTarget`
- `syncUnitStats`
- 卡牌上的等级/经验显示
- 本文档中的阈值表

装备与出售交互发生变化时，必须同时检查：

- `getUnitEquipment`
- `createEquipmentFromCard`
- `buyEquipmentToLineup`
- `moveOrSwapEquipment`
- `sellEquipment`
- `sellUnit`
- `createHeroCardMarkup`
- `renderShop`
- `renderLineup`
- 全局 `pointermove`、`pointerup`、`pointercancel` 拖拽处理
- `index.html` 的 `sellZone`
- `styles.css` 的装备槽与出售区域样式
- `README.md` 与本文档

计策拖拽交互发生变化时，必须同时检查：

- `game-data.js` 中计策的 `targetMode`
- `useStratagemOnLineup`
- `markLineupDropTarget`
- `createItemCardMarkup`
- `renderShop`
- 全局 `pointerup` 拖拽处理
- `README.md` 与本文档

拖拽可见表现发生变化时，必须同时检查：

- `startDragPreview`
- `updateDragPreview`
- `showDragPreview`
- `clearDragPreview`
- 全局 `pointermove`、`pointerup`、`pointercancel`
- `styles.css` 的 `.drag-source-follow` 相关样式
- `README.md` 与本文档

商店锁定与刷新行为发生变化时，必须同时检查：

- `createCardFromBase`
- `buildShop`
- `refreshShop`
- `renderShop`
- `createHeroCardMarkup`
- `createItemCardMarkup`
- `styles.css` 的 `.shop-card-lock-overlay` 样式
- `README.md` 与本文档

## 8. 升级奖励、蜀羁绊与暂未接入内容

网页已接入升级奖励的基础流程：

- 武将等级提升时触发奖励三选一；
- 奖励阶级为当前商店阶级 +1，最高不超过 6 阶；
- 从目标阶级的完整武将池随机生成 3 名不同候选；
- 候选生成与升级武将及当前阵容的羁绊无关；
- 玩家选择的武将以费用为 3 金币的奖励卡进入商店；选择候选时不扣费，之后拖入阵容购买时按普通武将流程检查并扣除 3 金币；
- 玩家可以点击“跳过”放弃本次奖励，跳过时不生成奖励卡；
- 三名候选使用与商店武将一致的完整卡牌样式显示，奖励弹窗不使用全屏半透明背景覆盖商店；
- 一次合并跨越多个等级时，每提升 1 级分别生成 1 次奖励。
- 普通商店操作产生升级时，先播放升级表现，再播放该次升级触发的技能或羁绊加成，最后展示奖励选择。
- 回合结束技能产生升级时，本回合只播放升级及其完整技能效果，不展示奖励选择；效果播放完成后直接进入战斗，奖励延后到下一回合商店刷新完成后再选择。

网页已接入“蜀汉再兴”的“武将升级时”效果：

- 羁绊等级按升级事件发生时所在阶段读取；
- 商店阶段升级会永久修改商店阵容：1/2 级蜀羁绊使升级武将分别获得 `+1/+1`、`+2/+2`，3/4 级使当前全军分别获得 `+1/+1`、`+2/+2`；
- 一次合并跨越多个等级时，每提升 1 级分别触发 1 次蜀羁绊效果；
- 升级触发蜀羁绊时，先在升级武将卡上播放等级提升表现，再在所有受影响武将卡上播放加成浮字；普通商店升级在浮字结束后显示奖励弹窗，回合结束升级则把奖励延后到下一回合；
- 进入战斗前锁定本场蜀羁绊等级，战斗中的阵亡、召唤和羁绊变化不再重算；
- 战斗结算底座支持武将技能带来的战斗阶段升级。战斗内升级只触发战斗副本中的升级、羁绊和武将技能，不生成商店升级奖励。

商店阶段作用于阵容武将的已接入正向属性加成使用统一加成浮字，覆盖属性成长计策、开悟产生的同名卡等价 `+1/+1` 和蜀羁绊。战斗阶段不播放属性加成浮字；魏羁绊、战斗蜀羁绊、群羁绊伤害加成、装备增伤和重骑兵攻击前成长仍正常结算，并通过逐轮战报展示结果。整军经武直接修改商店卡生成加成，不播放阵容浮字；玩家主动合并卡牌产生的堆叠成长也不播放该浮字。

以下升级相关效果暂不接入：

- 正式升级音效及更完整的粒子动画；
- 装备提供固定攻击/生命等直接属性的扩展；
- 状态槽及其直接属性在合并时的剥离、继承与回加；
- 多装备栏、物品库存和卸装回库存流程。

当前已经接入基础合并属性：

- 合并本体属性取双方各项最大值，合并本身不额外增加本体属性；
- 堆叠成长为 `（累计张数 + 直接经验 - 1）×（攻击+1、生命+1）`；
- 网页基础属性读取“武将”工作表的对应数值；
- 直接属性使用独立修改器结构，不混入本体比较；
- 攻击下限为 1、上限为 50，生命上限为 50。

## 9. 文档同步依据

后续 Harness 或 Agent 处理本功能时，按以下优先级和同步范围执行：

1. 用户在当前任务中的明确决定优先级最高。
2. 武将、装备与计策的具体阶级、描述和基础数据，以 `游戏数据速查.md` 为唯一权威来源；已删除的 `羁绊系统.xlsx` 不得恢复或使用。
3. 商店武将卡的金币、攻击和生命区域，以 `D:\三国自走棋\empire_gambit_client\Assets\Prefab\Draft\HeroCard0.prefab` 中对应节点为参考依据；阵容武将卡相同区域及装备槽，以同目录的 `HeroCard2.prefab` 为参考依据。
4. 本文档是网页 Demo 的升级、装备、换装、计策拖拽目标、出售交互和武将卡关键区域布局规则；`README.md` 负责同步说明当前已实现范围和运行口径。
5. `app.js`、`index.html`、`styles.css` 只能证明网页当前行为，不能反向覆盖本文档中的设计规则。
6. 修改攻血与金币布局、装备槽数量、购买目标、移动/交换逻辑、计策目标分类、计策拖拽、拖拽持续可见表现、出售区域、出售收益或合并继承规则时，必须同步更新本文档、`README.md`、相关网页代码和 `.harness/CHANGELOG.md`；涉及卡牌数据或计策分类时还要同步 `game-data.js`。
7. 如果文档权威范围、依赖或任务关键词发生变化，还必须同步 `.harness/manifest.json` 与 `.harness/routes.json`，并运行 `.harness/validate.ps1`。
8. 已接入效果的通用结算顺序不得在本交互文档中另行定义；统一以《技能结算规则.md》为准。
