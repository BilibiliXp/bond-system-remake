---
doc_id: gameplay.skill-resolution
title: 技能结算规则
status: active
authority: primary
authoritative_for:
  - 通用技能事件与触发时机
  - 同时攻击结算顺序
  - 同时机武将技能动态排序
  - 效果队列与连锁触发
  - 战斗伤害管线
not_authoritative_for:
  - 武将、装备、计策和羁绊的具体数值
  - 卡牌升级与交互
---

# 技能结算规则

## 1. 目的与权威边界

本文定义结构化技能事件、结算顺序、伤害管线、状态槽、召唤和复杂技能语义。武将、装备、计策与羁绊的名称和具体数值唯一以《游戏数据速查.md》为准；网页当前行为只能证明实现现状。

所有正式效果通过稳定 `effectId` 执行，不解析中文技能描述。

## 2. 通用术语

- **技能拥有者**：效果定义所属武将。
- **友军**：技能拥有者一方的其他单位，不包含拥有者本人；另一张同名武将仍是合法友军。
- **全军**：系统范围，包含触发者本人。
- **普通伤害**：`attack` 普攻伤害或 `skill` 技能普通伤害，可以被普通增减伤修改。
- **真实伤害**：`true`，不受普通增减伤影响。
- **当前攻血**：触发时战斗单位的当前攻击与当前生命。
- **最终面板攻血**：商店阵容卡面在当时显示的最终攻击与生命。
- **本轮**：当前一次前排同时交锋。

## 3. 结构化效果定义

每个效果至少包含：

- 稳定 `effectId`；
- 一个或多个触发事件；
- 目标合法性判断；
- 同时机优先级；
- 使用统一操作完成伤害、属性、经验、状态、召唤、阵亡或羁绊变化。

事件候选在时机打开时取得。已经取得的战斗开始或攻击后候选，不因拥有者在实际执行前阵亡而自动取消；效果文本另有前置条件时除外。

## 4. 事件与阶段

### 4.1 商店事件

| 事件 | 含义 |
|---|---|
| `round:start` | 新商店回合开始 |
| `round:end` | 玩家结束商店操作，即将创建战斗副本 |
| `card:purchase` | 购买任意商店卡成功 |
| `unit:recruit` | 购买武将成功，包括直接合入同名武将 |
| `unit:summon` | 新武将实例进入阵容空位；直接合入不产生 |
| `unit:upgrade` | 武将实际提升 1 个等级 |
| `unit:sell` | 出售武将 |
| `unit:death` | 商店阶段技能击杀成功 |
| `bond:count-change` | 一次操作前后至少一个羁绊的不同名人数发生变化 |
| `stratagem:use` | 计策目标和附加选择全部确认后成功使用 |

同一次操作即使改变多个羁绊人数，每名监听者也只接收 1 次 `bond:count-change`。

### 4.2 战斗事件

| 事件 | 含义 |
|---|---|
| `battle:start` | 羁绊效果锁定后、第一次交锋前 |
| `attack:before` | 本轮双方伤害快照前 |
| `attack:after` | 本轮同时扣血完成后 |
| `damage:before` | 单段伤害最终化前 |
| `damage:after` | 单段大于 0 的伤害落地后 |
| `status:apply` | 负面状态成功写入或替换后 |
| `unit:summon` | 战斗中创建新单位 |
| `unit:upgrade` | 战斗副本中的单位实际升级 |
| `unit:death` | 单位生命归零并进入阵亡处理 |

## 5. 同时攻击

一次基础交锋按以下顺序结算：

1. 读取双方当前最前方存活单位；
2. 打开 `attack:before`，按规则依次执行双方候选；
3. 重新读取两名攻击者当前攻击与状态，分别建立普通攻击伤害；
4. 两份伤害都完整通过 `damage:before`；
5. 同时扣除双方生命；
6. 分别派发实际伤害的 `damage:after`；
7. 统一处理本次同时扣血产生的阵亡；
8. 打开 `attack:after`，结算已经取得的武将、装备和状态候选；
9. 结算每次普通攻击完成后的灼烧伤害；
10. 进入下一轮。

基础同时攻击的两份伤害是唯一默认同时扣除的情况。技能的多段伤害逐段完整结算伤害、受伤、阵亡和衍生事件后，再执行下一段。

马超【破阵】的额外伤害并入首次普通攻击同一伤害段：1/2/3 级的该次普攻总倍率为当前攻击的 200%/300%/400%，不创建独立技能伤害段。

## 6. 同时机动态排序

跨类型固定优先级先判定：`battle:start` 时，东吴业火以最高优先级结算，必须先于双方所有武将、装备、状态与其他系统候选。令箭只提升相同触发条件下武将技能之间的顺序，不能越过东吴业火。

同一时机的武将技能按以下键排序：

1. 令箭等明确的特殊优先级；
2. 效果固有优先级；
3. 技能实际结算前重新读取拥有者当前攻击，攻击高者先；
4. 仍相同则使用稳定的阵容位置与效果编号顺序。

每结算一个候选后，剩余候选重新读取当前攻击再排序。装备、状态和羁绊效果使用各自固定优先级，不占用武将技能之间的攻击力比较规则。

## 7. 伤害管线

每段伤害记录 `source`、`target`、`type`、`sourceEffectId`、原始值、修改器和最终值。顺序为：

1. 伤害来源建立原始值；
2. 进攻方修改；
3. 防守方修改；
4. 装备与状态等特殊修改；
5. 向下取整并限制最低为 0；
6. 扣除生命；
7. 实际伤害大于 0 时派发 `damage:after`；
8. 检查阵亡。

破胆只增加 `attack` 与 `skill` 的普通伤害，不增加 `true` 真实伤害。畏惧只减少拥有者造成的 `attack` 普攻伤害，不减少技能或真实伤害。绝影、蚩尤古盾等普通减伤也不作用于真实伤害。铁盾与诏书写明“受到伤害”而未限定伤害类型，因此可以抵挡普通伤害与真实伤害。

### 7.1 奖励装备的伤害修正

- 方天画戟只在佩戴者参与普通攻击的同一次攻击阶段生效。建立双方普攻伤害时，佩戴者造成的普攻伤害增加 `向下取整（佩戴者当前攻击力 × 30%）`，佩戴者受到的对方本次普攻伤害减少同一数值，最低为 0；它不是跨整场战斗持续的通用减伤。
- 黄天令旗只修改佩戴者作为来源的 `skill` 普通伤害。每一段技能伤害分别附加 `向下取整（佩戴者当前攻击力 × 20%）`；不增加普攻或真实伤害。
- 铁盾在每场战斗第一次收到任意伤害段时抵挡最多 10 点，伤害不足 10 点时仍消耗；结算后立即从本场战斗副本的装备槽移除。

伤害事件必须同时保存伤害来源单位与 `sourceEffectId`。武将佩戴的装备直接造成伤害时，伤害来源单位仍是该佩戴者；装备施加的状态后续造成伤害时，也沿用施加当时的佩戴者归属。颜良等“击杀后”效果据此判断击杀者，普攻、武将技能、归属于该武将的状态伤害和其装备伤害均可计入。

张辽【威震逍遥】不是伤害：对敌军全体直接把当前生命改为 `向下取整（当前生命 ×（1 - 20% × 张辽等级））`，1/2/3 级即保留 80%/60%/40%。因此不触发受伤、伤害修改或减伤；生命降至 0 时正常进入阵亡流程。

## 8. 单状态槽

每名武将只有 1 个状态槽。灼烧、破胆、畏惧、连锁、震慑、反间、无双和休整占用该槽，任何时刻最多存在 1 个状态。技能禁用不是状态，不占用状态槽，而是独立的本轮技能触发限制。

- 新状态成功写入时覆盖旧状态，相同状态也不叠层；
- 无双和休整属于正面状态；前六种状态属于负面状态；
- 无双是唯一覆盖特例：当前状态为无双时，新的负面状态无法写入，不覆盖无双，也不产生成功的 `status:apply` 事件；
- 新的正面状态可以覆盖无双；新获得无双也会覆盖当时存在的任意其他状态；
- 技能禁用只持续当前交锋，到下一轮开始前独立清除；它与状态槽中的状态可以同时存在，彼此不覆盖，也不触发 `status:apply`、灼烧引燃、无双免疫、休整拦截或诏书的状态拦截；
- 百厄角只从灼烧、破胆、畏惧、连锁、震慑、反间六种负面状态中等概率随机 1 种，不会随机到技能禁用。

### 8.1 灼烧与引燃

- 每次任意普通攻击完成后，每个仍拥有灼烧的单位受到 1 点真实伤害。
- 已有灼烧的单位再次获得任意负面状态时，新状态先写入单状态槽并覆盖旧灼烧；被覆盖的旧灼烧随后触发 6 点真实引燃伤害。
- 引燃完成后，只在状态槽当前仍为灼烧时清除该灼烧；当前为其他负面状态时不清除。因此“灼烧覆盖灼烧”会清除新写入的灼烧，通常以空状态槽结束；“震慑覆盖灼烧”则保留震慑。
- 东吴业火 5 人效果只在上述引燃实际清除了状态槽中的当前灼烧后，重新施加其规定的己方原灼烧；例如“灼烧覆盖灼烧”时，新灼烧先被引燃清除，再由 5 人效果补回原灼烧。当前为震慑等其他负面状态时没有灼烧被清除，因此不补回灼烧。

### 8.2 破胆与畏惧

- 破胆：目标受到的普通攻击和技能普通伤害 +5。
- 畏惧：目标造成的普通攻击伤害 -3，最低为 0。

### 8.3 震慑

- 未消耗的震慑使下一次普通攻击伤害变为 0。
- 触发后不清除状态，只将其标记为已消耗。
- 已消耗且仍存在的同一个震慑不能被重复施加刷新。
- 只有先被其他负面状态覆盖，再重新获得震慑，才会再次使下一次普攻为 0。

### 8.4 连锁

连锁单位受到其他负面状态时，必须原子处理：

1. 先快照战场上全部连锁单位；
2. 再把快照内所有单位的连锁统一替换为新状态；
3. 该批替换可以产生正常状态事件，但不得再次触发连锁传播。

禁止边遍历边替换，避免递归或漏目标。

### 8.5 反间

反间单位阵亡后，在施加者一方最前方以 LV1、3/3 召唤该单位。召唤体不继承阵亡单位的装备、羁绊或状态；其羁绊按通用召唤规则继承反间施加者触发施加时的全部当前羁绊。

### 8.6 周瑜放大范围

周瑜【风助火势】只放大明确数值：灼烧每次伤害 1、引燃 6、破胆 +5、畏惧 -3。1/2/3 级周瑜使这些值为基础值的 2/3/4 倍。

周瑜不放大目标数、触发次数、持续次数，也不改变反间、连锁、震慑等规则性效果。

### 8.7 休整与诏书拦截

- 休整是正面状态。佩戴者将获得负面状态时，先清除休整并阻止该次传入的负面状态；不派发一次成功的 `status:apply`。
- 诏书是装备而非状态。佩戴者将获得负面状态时，先清空其当前状态槽，再移除诏书，并阻止该次传入的负面状态；不派发一次成功的 `status:apply`。
- 同一单位同时具有休整与诏书时，诏书优先拦截，并按上条同时清除休整和诏书。
- 诏书受到伤害时抵挡最多 10 点并立即移除；例如 15 点伤害最终保留 5 点。该拦截不区分普通伤害与真实伤害。
- 白玉龟不阻止负面状态的成功写入；它在 `status:apply` 后立即清除佩戴者当前收到的负面状态，并使佩戴者 +2/+2。若负面状态未成功写入，则白玉龟不触发。

## 9. 召唤、阵亡与位置

- “召唤到拥有者身前”以拥有者当时位置为锚点；拥有者已阵亡时使用其死亡相对位置。
- 队伍无空位时召唤失败；同时攻击产生空位后，阵亡技能可以使用该空位。
- 除复活外，召唤物统一继承召唤者触发召唤时的全部当前羁绊，包括初始、永久额外和临时羁绊；不继承召唤者的装备或状态。复活不是召唤继承，固定沿用死者的羁绊快照。
- 所有拥有魏羁绊的战斗单位阵亡都计入魏武遗风，包括召唤骑兵。
- 帅印监听己方 `unit:summon`。正式武将、骑兵、重骑兵及其他衍生武将都视为武将并获得 +1/+1；复活不属于召唤，不触发帅印。
- 阎魔帆在佩戴者阵亡后按其死亡位置复活该单位，等级沿用死者等级，攻血固定为 5/5，羁绊沿用死者快照，但不继承装备与状态。复活体不再携带阎魔帆，因此不会由同一件装备反复复活。

普通骑兵基础 2/1、无技能。马云禄召唤的骑兵等级等于马云禄等级，1/2/3 级为 2/1、4/2、6/3。夏侯渊召唤的骑兵始终为 LV1，其攻血分别读取触发时夏侯渊当前攻血的 33%/66%/99%，向下取整且各自最低为 1。两者都继承对应召唤者的全部当前羁绊。

于吉阵亡后，方士插入于吉阵亡前的原位置，而不是队首或于吉身前。方士等级等于于吉等级，1/2/3 级攻血为 1/1、2/2、3/3；阵亡时使随机 1 名其他存活友军 +3/+3、+6/+6、+9/+9。

张郃阵亡时，从所有与张郃至少共享一个当前羁绊的正式武将中随机选择，不限阶级并排除张郃。召唤物保留抽中武将的名称与技能，等级等于张郃等级，基础攻血乘等级；羁绊统一继承张郃触发召唤时的全部当前羁绊，不带装备或状态。

## 10. 随机与事件链

- 战斗随机必须使用本场种子随机源，不得直接调用不可复现随机。
- “随机 N 名”默认不放回；合法目标不足 N 名时取全部合法目标。
- “人数最多的已激活羁绊”比较不同名人数；并列时随机选择 1 个。
- 效果产生的新事件可以继续入队，但每条事件链有硬上限；达到上限时终止该链并记录错误，不能卡死页面。
- 重复状态、连锁传播和复活必须有显式去重或次数边界。

## 11. 商店阵容与战斗副本

结束商店回合时，先完成所有 `round:end` 效果，再创建战斗副本。创建后锁定本场羁绊人数效果，并复制单位当时的攻血、等级、羁绊、装备与持久状态。

龙方壶在 `round:end` 读取当时 5 格阵容的羁绊人数；不存在任何达到 2 人效果的激活羁绊时，使携带者的商店本体永久 +2/+2。虎符则在战斗副本的 `battle:start` 使佩戴者 +3/+3 并获得 1 点经验，属性和经验均不回写商店阵容。

战斗副本中的成长、阵亡、召唤、复活和状态不回写商店阵容；传国玉玺等明确跨阶段效果除外。战斗内阵亡和召唤不会重新计算已经锁定的羁绊人数效果。

有明确使用次数的装备在战斗副本中完成最后一次有效结算后，立即从该战斗单位的装备槽移除，之后按无装备处理，不保留或显示 `0` 次装备。该移除只作用于本场战斗副本，不回写商店阵容。

## 12. 已确认的复杂技能口径

- **陈宫**：被招募时，生成与陈宫等级同阶的计策并作用于陈宫最近的前方武将；若计策需要选择羁绊，弹出不可随机替代的玩家选择界面，完成选择前流程暂停。
- **左慈**：出售时读取前后最近且阶级不高于 2/4/6 的两名武将，直接交换其当时最终面板攻血；两名目标无需共享羁绊，装备、状态、经验、羁绊和其他身份数据留在原单位。
- **韩当**：攻击前从当前所有拥有负面状态的敌军中随机选择 1 名；没有合法目标时不触发。
- **司马徽**：每次购买司马徽时触发，不是购买任意商品；必须选择 1 名其他友军添加 1 个额外羁绊，不给予属性，效果不随司马徽等级变化。
- **董卓**：回合结束时必须成功击杀合法目标后，才获得自身属性加成；无合法目标或击杀失败均不加。
- **华佗**：前方最近的友军阵亡时，固定以 LV2、3/3 复活；复活体完整沿用阵亡单位当时的羁绊快照，不读取华佗的羁绊。每名华佗在同一游戏回合内最多成功复活 1/2/3 次，升级只改变次数；复活失败不消耗次数。
- **魏延**：任意其他友军阵亡时触发；魏延自己阵亡不触发自己的技能。
- **文丑**：最近的前方友军完成普通攻击后，自身 +1/+1、+2/+2、+3/+3。
- **颜良**：只要阵亡单位的最终击杀归属于颜良，颜良便 +2/+2、+4/+4、+6/+6；装备作为伤害媒介时仍把击杀归于佩戴者颜良。
- **汉献帝**：阵亡时只从装备槽为空的其他存活友军中随机选择诏书目标；没有合法目标时不覆盖或销毁已有装备。
- **荀攸**：每次计策成功使用后，分别重新读取最近的前方 1 名与最近的后方 1 名友军并加成；缺少一侧不影响另一侧。
- **甘宁**：战斗开始时执行 2/4/6 次“施加灼烧”，不是强制触发灼烧伤害。每次施加都完整经过覆盖与引燃规则，并在下一次施加前重新读取当前最后方存活敌军；原目标被引燃击杀后，剩余次数继续转向新的最后方敌军。
- **袁术**：只在真正的 `unit:summon` 事件中获得传国玉玺；直接合并招募和复活均不触发。战斗中被召唤的袁术也可触发。
- **刘备**：商店或战斗中的同羁绊单位被真正召唤时获得 1/2/3 点经验；直接合并和复活不触发。
- **孙策**：统计其他自身羁绊武将，不把自己计入。
- **吕布**：1/2/3 级无双作用于普通攻击目标后方最近的 1/2/3 名单位，追加伤害比例始终为本次普攻最终伤害的 30%。
- **凌统及所有友军技能**：不作用于技能拥有者自身，但另一张同名武将是合法友军。

## 13. 经验、属性与控制

- 经验逐点结算，每获得 1 点经验都先更新本体堆叠，再检查升级。
- 一次效果跨越多个等级时，每次实际升级分别派发 `unit:upgrade`。
- 单位面板攻击和生命上限最低为 1；伤害结算值最低为 0；当前生命不得高于生命上限。
- 吕蒙的“本轮无法触发技能”只禁用武将技能，不禁用装备、羁绊、状态和系统效果。
- 无双不叠加；休整由其他友军完成交锋时触发，拥有者自己的交锋不触发自身休整。休整每次按法正等级恢复 2/4/6 点生命，并按 8.7 节拦截负面状态。

## 14. 验收要求

实现至少应验证：

- 60 名正式武将与稳定效果 ID 可加载；
- 同时攻击前后快照连续；
- 5 人效果在锁定/解锁时分别回退到 4 人/使用 5 人；
- 全部状态共用一个槽；无双免疫后续负面状态，正面状态可以覆盖无双，获得无双会覆盖已有状态；灼烧覆盖灼烧时引燃后清除当前灼烧，其他负面状态覆盖灼烧时保留新状态，东吴业火 5 人只在当前灼烧实际被清除后补回原灼烧；连锁原子替换，震慑保留已消耗标记；
- 马超额外伤害属于同一普攻段；
- 夏侯渊百分比向下取整、最低 1；
- 张郃召唤池、等级、属性和清除项正确；
- 友军效果排除拥有者，孙策不统计自身；
- 固定战斗种子可以复现随机目标、状态和结果。
